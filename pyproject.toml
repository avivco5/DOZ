[project]
name = "friendly-direction-warning-poc"
version = "0.1.0"
description = "Friendly direction warning game demo coordinator"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
